# v0a pipeline dependencies
# Python 3.12 or 3.13 (gensim does not yet ship wheels for 3.14).
#
# Version notes:
# - gensim 4.x pulls in numpy/scipy; on some setups gensim requires numpy < 2,
#   so numpy is pinned below to avoid the incompatibility.
# - openpyxl is required by pandas.read_excel() to read the .xlsx dataset.

pandas>=2.0
openpyxl>=3.1          # pandas needs this to read .xlsx files
gensim>=4.3,<5.0
numpy>=1.24,<2.0       # gensim compatibility
scipy>=1.10
scikit-learn>=1.3      # PCA in harness/visualize.py
matplotlib>=3.7        # plotting in harness/visualize.py